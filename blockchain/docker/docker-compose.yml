version: "3.9"
services:
  node:
    build:
      context: ..
      dockerfile: docker/Dockerfile.node
    ports:
      - "8080:8080"
  client:
    build:
      context: ..
      dockerfile: docker/Dockerfile.client
    depends_on:
      - node
